'use strict';

/**
 * תאימות לאחור — אחסון המצב עבר למודול src/state-store.js (Upstash Redis).
 * קובץ זה מייצא מחדש את אותו ה-API כדי לא לשבור ייבואים קיימים.
 */

module.exports = require('./state-store');
